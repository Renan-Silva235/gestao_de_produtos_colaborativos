# gestao_de_produtos_colaborativos
Esse projeto é a continuação do projeto da faculdade de gestão de produtos colaborativos, mas implementando um frontend e backend com banco de dados Sqlite e o framework Express


## 🚀 Configuração Inicial do Projeto (Windows)

Este guia rápido tem o objetivo de configurar o ambiente de desenvolvimento e instalar as dependências do projeto.

### 1. Instalação do Node.js e NPM

O Node.js é essencial e já inclui o npm (Node Package Manager).

* **Download:** Clique no link abaixo e baixe o instalador LTS (Recomendado):
    * 🔗 **[Link para Download do Node.js](https://nodejs.org/en/download/)**

* **Instalação:** Execute o arquivo `.msi` baixado e siga o assistente de instalação, mantendo as opções padrão.

* **Verificação:** Após a instalação, **reinicie o seu terminal (CMD/PowerShell)** e execute:

    ```bash
    node -v
    npm -v
    ```

    Isso deve mostrar as versões instaladas.

---

### 2. Instalação das Dependências do Projeto

Com o Node.js e npm configurados, instale os pacotes necessários:

* **Passo 1:** No seu terminal, navegue até a pasta principal do projeto.

* **Passo 2:** Execute o comando de instalação:

    ```bash
    npm i
    ```

Este comando baixa todas as dependências listadas no `package.json`. Pronto!
